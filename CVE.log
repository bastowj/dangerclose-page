# CVE log

Audit findings and what was decided. Current state: `pnpm audit`.
Transitive pins are raised via `pnpm.overrides` in `package.json`.

## 2026-09-12 — resolved 7 findings

Updated the direct framework and image dependencies, then refreshed the lockfile.
`next` and `eslint-config-next` resolve to 16.3.5 (within the new `^16.3.3`
range) and `sharp` resolves to 0.35.4. The scoped `js-yaml` overrides now
select 3.15.2 and 4.3.2. `remark-mdx-frontmatter` has no patched 3.x TOML
release, so its transitive `toml` dependency is overridden to 4.3.0 through
the compatible `toml: ^4.2.0` override; the parse API used by that dependency
remains compatible.

| Package | From → To | How | Advisories resolved |
| --- | --- | --- | --- |
| next | 16.3.2 → 16.3.5 | dependency refresh | CVE-2026-75604 (GHSA-p293-qw3h-jr36); GHSA-2xp9-vwfh-vxw4 |
| sharp | 0.35.3 → 0.35.4 | dependency refresh and override | GHSA-rgj7-g3m4-5g8c (libheif; CVE-2026-84383) |
| js-yaml | 3.15.1 → 3.15.2; 4.3.1 → 4.3.2 | scoped overrides | CVE-2026-84375 (GHSA-2883-xcg3-v3hh) |
| toml | 3.0.0 → 4.3.0 | override | CVE-2026-77465 (GHSA-82x6-q7mm-w9cf); CVE-2026-63376 (GHSA-v5mp-jgw5-2x6j) |

Confirmed clean: `pnpm audit` reports 0 vulnerabilities. `pnpm lint` and
`pnpm typecheck` pass. `pnpm test` currently exits 1 because Jest finds no
tests; this is an existing repository test-script condition, not a failure in
the dependency update.

## 2026-08-23 — Vercel build compatibility and dependency refresh

Updated `next` and `eslint-config-next` from 16.2.12 to 16.3.2. The Vercel
adapter cannot be combined with Next's `output: "standalone"` copy step: it
consumes the trace files during packaging, after which Next's standalone step
can fail reading the server trace. `next.config.ts` now disables standalone
output only when `NEXT_ADAPTER_PATH` or `VERCEL` is set; Docker and local
production builds retain it.

Refined the advisory overrides so they upgrade only compatible major lines:
brace-expansion 1.x and 2.x, js-yaml 3.x and 4.x, and nanoid 3.x. In
particular, brace-expansion 5.x remains untouched because forcing it to 1.x
breaks ESLint's bundled minimatch dependency. Confirmed clean with `pnpm audit`.

## 2026-08-14 — resolved 8 findings

Superseded by the scoped, compatible-major overrides recorded on 2026-08-23.

Resolved via `pnpm.overrides` in `package.json`. All 3 existing brace-expansion versions (1.1.16, 2.1.2, 5.0.8) replaced with 1.1.18 — pnpm applies overrides universally. This is safe because brace-expansion 1.x and 2.x share the same function export API (only 5.x changed to named `{ expand }` exports, which we do NOT use).

| Package      | From → To              | How      | Advisories resolved                                                 |
| ------------ | ---------------------- | -------- | ------------------------------------------------------------------- |
| brace-expansion | 1.1.16→1.1.18, 2.1.2→1.1.18, 5.0.8→1.1.18 | override | CVE-2026-14257, CVE-2026-59870 (all versions)                       |
| js-yaml      | 3.15.0→3.15.1, 4.3.0→4.3.1 | override | CVE-2026-59870 (GHSA-5p4m-2wfm-xmqj)                                |
| nanoid       | 3.3.16→3.3.18          | override | CVE-2026-45870 (GHSA-2v37-7h3g-55p8)                                |

Confirmed clean: `pnpm audit` reports 0 vulnerabilities. `pnpm lint`, `pnpm typecheck`, and `pnpm test` all pass.

## 2026-07-26 — added `@content-collections/cli`

No new findings. Added as a devDependency so `pnpm typecheck` can generate the
collections itself; audit unchanged at the two accepted paths above.

## 2026-07-26

7 of 7 findings resolved, then a dependency removal cleared an eighth path.

| Package         | From → To         | How                              | Advisories                                                                        |
| --------------- | ----------------- | -------------------------------- | --------------------------------------------------------------------------------- |
| next            | 16.2.10 → 16.2.12 | version bump                     | patch-level, in range                                                             |
| postcss         | 8.4.31 → 8.5.23   | override, `next` pins it exactly | 3 (1 moderate, 2 high)                                                            |
| sharp           | 0.34.5 → 0.35.3   | override, `next` pins `^0.34.5`  | 1 high (libvips)                                                                  |
| uuid            | 9.0.1 → 11.1.1    | override, `mdx-bundler` pins 9   | 1 moderate, GHSA-w5hq-g745-h8pq (missing buffer bounds check in v3/v5/v6)          |
| brace-expansion | —                 | dropped `@eslint/eslintrc`       | removed the `eslintrc>minimatch` path; see Open for the two that remain           |

Rejected upgrades, for the record — both break the build, so they are not audit workarounds:

- **eslint 9 → 10**: `eslint-config-next` bundles `eslint-plugin-react@7.37.5`, which crashes on ESLint 10 (`contextOrFilename.getFilename is not a function`).
- **typescript 6 → 7**: `ts-jest` declares `typescript: ">=4.3 <7"`.
